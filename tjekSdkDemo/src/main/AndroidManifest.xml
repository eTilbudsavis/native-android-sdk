<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    package="com.tjek.sdk.demo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".DemoApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <!-- Demo keys -->
        <meta-data
            android:name="com.tjek.sdk.api_key"
            android:value="00ibm8b65trmxkmo6fy42nr5hyesj8o9"/>
        <meta-data
            android:name="com.tjek.sdk.develop.api_key"
            android:value="00ibm8b65trmxkmo6fy42nr5hyesj8o9"/>

        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".publication.PublicationListActivity" />
        <activity android:name=".publication.PagedPublicationActivity" />
        <activity android:name=".publication.IncitoPublicationActivity" />

    </application>

</manifest>